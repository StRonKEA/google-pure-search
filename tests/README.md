# Google Clean Search regression tests

Tests run the complete userscript in a real browser against controlled DOM fixtures.
Live Google checks are recorded separately; fixture results do not establish compatibility with every Google experiment or userscript manager.
